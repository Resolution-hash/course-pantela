1. Обновить `POST` ручку, чтобы она записывала содержимое `message` в БД 
2. Обновить `GET` ручку, чтобы она выводила слайс `message` (все message, которые лежат в БД)
3. Сделать коммит и загрузить изменения в репозиторий который вы создавали под проект

Для этого вам потребуется узнать о методах и работе с ORM, также о том, как передается и хранится JSON